import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ec.com.cnt.juntas',
  appName: 'juntas',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
