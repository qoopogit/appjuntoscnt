import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.juntas.starter',
  appName: 'juntas',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
